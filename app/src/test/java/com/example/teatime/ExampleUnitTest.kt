package com.example.teatime

import org.junit.Assert.assertEquals
import org.junit.Test

class ExampleUnitTest {

    @Test
    fun `Adding two numbers returns the expected result`() {
        assertEquals(4, 2 + 2)
    }
}